// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

import Foundation

// === xdr source ============================================================

//  struct DataOwnerUpdateRequest {
//      UpdateDataOwnerOp updateDataOwnerOp;
//  
//      //: Sequence number increases when request is rejected
//      uint32 sequenceNumber;
//  
//      //: Arbitrary stringified json object that can be used to attach data to be reviewed by an admin
//      longstring creatorDetails; // details set by requester
//  
//      //: reserved for future use
//      union switch (LedgerVersion v)
//      {
//      case EMPTY_VERSION:
//          void;
//      }
//      ext;
//  };

//  ===========================================================================
public struct DataOwnerUpdateRequest: XDRCodable {
  public var updateDataOwnerOp: UpdateDataOwnerOp
  public var sequenceNumber: Uint32
  public var creatorDetails: Longstring
  public var ext: DataOwnerUpdateRequestExt

  public init(
      updateDataOwnerOp: UpdateDataOwnerOp,
      sequenceNumber: Uint32,
      creatorDetails: Longstring,
      ext: DataOwnerUpdateRequestExt) {

    self.updateDataOwnerOp = updateDataOwnerOp
    self.sequenceNumber = sequenceNumber
    self.creatorDetails = creatorDetails
    self.ext = ext
  }

  public func toXDR() -> Data {
    var xdr = Data()

    xdr.append(self.updateDataOwnerOp.toXDR())
    xdr.append(self.sequenceNumber.toXDR())
    xdr.append(self.creatorDetails.toXDR())
    xdr.append(self.ext.toXDR())

    return xdr
  }

  public init(xdrData: inout Data) throws {
    self.updateDataOwnerOp = try UpdateDataOwnerOp(xdrData: &xdrData)
    self.sequenceNumber = try Uint32(xdrData: &xdrData)
    self.creatorDetails = try Longstring(xdrData: &xdrData)
    self.ext = try DataOwnerUpdateRequestExt(xdrData: &xdrData)
  }

  public enum DataOwnerUpdateRequestExt: XDRDiscriminatedUnion {
    case emptyVersion

    public var discriminant: Int32 {
      switch self {
      case .emptyVersion: return LedgerVersion.emptyVersion.rawValue
      }
    }

    public func toXDR() -> Data {
      var xdr = Data()

      xdr.append(self.discriminant.toXDR())

      switch self {
      case .emptyVersion: xdr.append(Data())
      }

      return xdr
    }

    public init(xdrData: inout Data) throws {
      let discriminant = try Int32(xdrData: &xdrData)

      switch discriminant {
      case LedgerVersion.emptyVersion.rawValue: self = .emptyVersion
      default:
        throw XDRErrors.unknownEnumCase
      }
    }

  }
}
