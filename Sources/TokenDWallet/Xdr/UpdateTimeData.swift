// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

import Foundation

// === xdr source ============================================================

//  //: Details are valid if one of the fileds is not zero
//  struct UpdateTimeData {
//      //: start time can be updated if sale is not started yet (zero means no changes)
//      uint64 newStartTime; 
//      //: end time should be greater than start time (zero means no changes)
//      uint64 newEndTime;
//  
//      //: Reserved for future use
//      union switch (LedgerVersion v)
//      {
//      case EMPTY_VERSION:
//          void;
//      } ext;
//  };

//  ===========================================================================
public struct UpdateTimeData: XDRCodable {
  public var newStartTime: Uint64
  public var newEndTime: Uint64
  public var ext: UpdateTimeDataExt

  public init(
      newStartTime: Uint64,
      newEndTime: Uint64,
      ext: UpdateTimeDataExt) {

    self.newStartTime = newStartTime
    self.newEndTime = newEndTime
    self.ext = ext
  }

  public func toXDR() -> Data {
    var xdr = Data()

    xdr.append(self.newStartTime.toXDR())
    xdr.append(self.newEndTime.toXDR())
    xdr.append(self.ext.toXDR())

    return xdr
  }

  public init(xdrData: inout Data) throws {
    self.newStartTime = try Uint64(xdrData: &xdrData)
    self.newEndTime = try Uint64(xdrData: &xdrData)
    self.ext = try UpdateTimeDataExt(xdrData: &xdrData)
  }

  public enum UpdateTimeDataExt: XDRDiscriminatedUnion {
    case emptyVersion

    public var discriminant: Int32 {
      switch self {
      case .emptyVersion: return LedgerVersion.emptyVersion.rawValue
      }
    }

    public func toXDR() -> Data {
      var xdr = Data()

      xdr.append(self.discriminant.toXDR())

      switch self {
      case .emptyVersion: xdr.append(Data())
      }

      return xdr
    }

    public init(xdrData: inout Data) throws {
      let discriminant = try Int32(xdrData: &xdrData)

      switch discriminant {
      case LedgerVersion.emptyVersion.rawValue: self = .emptyVersion
      default:
        throw XDRErrors.unknownEnumCase
      }
    }

  }
}
