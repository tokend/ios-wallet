// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

import Foundation

// === xdr source ============================================================

//  //: Result of RemoveAssetPair operation application along with the result code
//  union RemoveAssetPairResult switch (RemoveAssetPairResultCode code) {
//      case SUCCESS:
//          RemoveAssetPairSuccess success;
//      default:
//          void;
//  };

//  ===========================================================================
public enum RemoveAssetPairResult: XDRDiscriminatedUnion {
  case success(RemoveAssetPairSuccess)

  public var discriminant: Int32 {
    switch self {
    case .success: return RemoveAssetPairResultCode.success.rawValue
    }
  }

  public func toXDR() -> Data {
    var xdr = Data()

    xdr.append(self.discriminant.toXDR())

    switch self {
    case .success(let data): xdr.append(data.toXDR())
    }

    return xdr
  }

  public init(xdrData: inout Data) throws {
    let discriminant = try Int32(xdrData: &xdrData)

    switch discriminant {
    case RemoveAssetPairResultCode.success.rawValue:
      let data = try RemoveAssetPairSuccess(xdrData: &xdrData)
      self = .success(data)
    default:
      throw XDRErrors.unknownEnumCase
    }
  }
}
