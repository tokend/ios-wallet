// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

import Foundation

// === xdr source ============================================================

//  //: Extended result of a review request operation containing details specific to a Create Atomic Swap Request
//  struct AtomicSwapBidExtended
//  {
//      //: ID of a ask to apply atomic swap to
//      uint64 askID;
//      //: AccountID of a ask owner
//      AccountID askOwnerID;
//      //: Account id of an bid owner
//      AccountID bidOwnerID;
//      //: Base asset for the atomic swap
//      AssetCode baseAsset;
//      //: Quote asset for the atomic swap
//      AssetCode quoteAsset;
//      //: Amount in base asset to exchange
//      uint64 baseAmount;
//      //: Amount in quote asset to exchange
//      uint64 quoteAmount;
//      //: Price of base asset in terms of quote
//      uint64 price;
//      //: Balance in base asset of a ask owner
//      BalanceID askOwnerBaseBalanceID;
//      //: Balance in base asset of an bid owner
//      BalanceID bidOwnerBaseBalanceID;
//      //: Amount which was unlocked on bid owner base balance after bid removing
//      uint64 unlockedAmount;
//  
//      //: Reserved for future use
//      union switch (LedgerVersion v)
//      {
//      case EMPTY_VERSION:
//              void;
//      }
//      ext;
//  };

//  ===========================================================================
public struct AtomicSwapBidExtended: XDREncodable {
  public var askID: Uint64
  public var askOwnerID: AccountID
  public var bidOwnerID: AccountID
  public var baseAsset: AssetCode
  public var quoteAsset: AssetCode
  public var baseAmount: Uint64
  public var quoteAmount: Uint64
  public var price: Uint64
  public var askOwnerBaseBalanceID: BalanceID
  public var bidOwnerBaseBalanceID: BalanceID
  public var unlockedAmount: Uint64
  public var ext: AtomicSwapBidExtendedExt

  public init(
      askID: Uint64,
      askOwnerID: AccountID,
      bidOwnerID: AccountID,
      baseAsset: AssetCode,
      quoteAsset: AssetCode,
      baseAmount: Uint64,
      quoteAmount: Uint64,
      price: Uint64,
      askOwnerBaseBalanceID: BalanceID,
      bidOwnerBaseBalanceID: BalanceID,
      unlockedAmount: Uint64,
      ext: AtomicSwapBidExtendedExt) {

    self.askID = askID
    self.askOwnerID = askOwnerID
    self.bidOwnerID = bidOwnerID
    self.baseAsset = baseAsset
    self.quoteAsset = quoteAsset
    self.baseAmount = baseAmount
    self.quoteAmount = quoteAmount
    self.price = price
    self.askOwnerBaseBalanceID = askOwnerBaseBalanceID
    self.bidOwnerBaseBalanceID = bidOwnerBaseBalanceID
    self.unlockedAmount = unlockedAmount
    self.ext = ext
  }

  public func toXDR() -> Data {
    var xdr = Data()

    xdr.append(self.askID.toXDR())
    xdr.append(self.askOwnerID.toXDR())
    xdr.append(self.bidOwnerID.toXDR())
    xdr.append(self.baseAsset.toXDR())
    xdr.append(self.quoteAsset.toXDR())
    xdr.append(self.baseAmount.toXDR())
    xdr.append(self.quoteAmount.toXDR())
    xdr.append(self.price.toXDR())
    xdr.append(self.askOwnerBaseBalanceID.toXDR())
    xdr.append(self.bidOwnerBaseBalanceID.toXDR())
    xdr.append(self.unlockedAmount.toXDR())
    xdr.append(self.ext.toXDR())

    return xdr
  }

  public enum AtomicSwapBidExtendedExt: XDRDiscriminatedUnion {
    case emptyVersion()

    public var discriminant: Int32 {
      switch self {
      case .emptyVersion: return LedgerVersion.emptyVersion.rawValue
      }
    }

    public func toXDR() -> Data {
      var xdr = Data()

      xdr.append(self.discriminant.toXDR())

      switch self {
      case .emptyVersion(): xdr.append(Data())
      }

      return xdr
    }

  }
}
