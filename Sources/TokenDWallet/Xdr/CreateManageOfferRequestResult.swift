// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

import Foundation

// === xdr source ============================================================

//  union CreateManageOfferRequestResult switch (CreateManageOfferRequestResultCode code) 
//  {
//  case SUCCESS:
//      CreateManagerOfferRequestSuccessResult success;
//  case INVALID_OFFER:
//      ManageOfferResultCode manageOfferCode;
//  default: 
//      void;
//  };

//  ===========================================================================
public enum CreateManageOfferRequestResult: XDRDiscriminatedUnion {
  case success(CreateManagerOfferRequestSuccessResult)
  case invalidOffer(ManageOfferResultCode)

  public var discriminant: Int32 {
    switch self {
    case .success: return CreateManageOfferRequestResultCode.success.rawValue
    case .invalidOffer: return CreateManageOfferRequestResultCode.invalidOffer.rawValue
    }
  }

  public func toXDR() -> Data {
    var xdr = Data()

    xdr.append(self.discriminant.toXDR())

    switch self {
    case .success(let data): xdr.append(data.toXDR())
    case .invalidOffer(let data): xdr.append(data.toXDR())
    }

    return xdr
  }

  public init(xdrData: inout Data) throws {
    let discriminant = try Int32(xdrData: &xdrData)

    switch discriminant {
    case CreateManageOfferRequestResultCode.success.rawValue:
      let data = try CreateManagerOfferRequestSuccessResult(xdrData: &xdrData)
      self = .success(data)
    case CreateManageOfferRequestResultCode.invalidOffer.rawValue:
      let data = try ManageOfferResultCode(xdrData: &xdrData)
      self = .invalidOffer(data)
    default:
      throw XDRErrors.unknownEnumCase
    }
  }
}
