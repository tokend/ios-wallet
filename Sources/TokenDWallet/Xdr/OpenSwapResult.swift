// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

import Foundation

// === xdr source ============================================================

//  //: OpenSwapResult is a result of OpenSwap operation application
//  union OpenSwapResult switch (OpenSwapResultCode code)
//  {
//  case SUCCESS:
//      OpenSwapSuccess success;
//  default:
//      void;
//  };

//  ===========================================================================
public enum OpenSwapResult: XDRDiscriminatedUnion {
  case success(OpenSwapSuccess)

  public var discriminant: Int32 {
    switch self {
    case .success: return OpenSwapResultCode.success.rawValue
    }
  }

  public func toXDR() -> Data {
    var xdr = Data()

    xdr.append(self.discriminant.toXDR())

    switch self {
    case .success(let data): xdr.append(data.toXDR())
    }

    return xdr
  }

  public init(xdrData: inout Data) throws {
    let discriminant = try Int32(xdrData: &xdrData)

    switch discriminant {
    case OpenSwapResultCode.success.rawValue:
      let data = try OpenSwapSuccess(xdrData: &xdrData)
      self = .success(data)
    default:
      throw XDRErrors.unknownEnumCase
    }
  }
}
