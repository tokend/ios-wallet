// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

import Foundation

// === xdr source ============================================================

//  //: ManageAccountSpecificRuleOp is used to create or remove account specific rule
//  struct ManageAccountSpecificRuleOp
//  {
//      //: data is used to pass one of `ManageAccountSpecificRuleAction` with required params
//      union switch (ManageAccountSpecificRuleAction action)
//      {
//      case CREATE:
//          CreateAccountSpecificRuleData createData;
//      case REMOVE:
//          RemoveAccountSpecificRuleData removeData;
//      } data;
//  
//      //: reserved for future use
//      union switch (LedgerVersion v)
//      {
//      case EMPTY_VERSION:
//          void;
//      }
//      ext;
//  };

//  ===========================================================================
public struct ManageAccountSpecificRuleOp: XDRCodable {
  public var data: ManageAccountSpecificRuleOpData
  public var ext: ManageAccountSpecificRuleOpExt

  public init(
      data: ManageAccountSpecificRuleOpData,
      ext: ManageAccountSpecificRuleOpExt) {

    self.data = data
    self.ext = ext
  }

  public func toXDR() -> Data {
    var xdr = Data()

    xdr.append(self.data.toXDR())
    xdr.append(self.ext.toXDR())

    return xdr
  }

  public init(xdrData: inout Data) throws {
    self.data = try ManageAccountSpecificRuleOpData(xdrData: &xdrData)
    self.ext = try ManageAccountSpecificRuleOpExt(xdrData: &xdrData)
  }

  public enum ManageAccountSpecificRuleOpData: XDRDiscriminatedUnion {
    case create(CreateAccountSpecificRuleData)
    case remove(RemoveAccountSpecificRuleData)

    public var discriminant: Int32 {
      switch self {
      case .create: return ManageAccountSpecificRuleAction.create.rawValue
      case .remove: return ManageAccountSpecificRuleAction.remove.rawValue
      }
    }

    public func toXDR() -> Data {
      var xdr = Data()

      xdr.append(self.discriminant.toXDR())

      switch self {
      case .create(let data): xdr.append(data.toXDR())
      case .remove(let data): xdr.append(data.toXDR())
      }

      return xdr
    }

    public init(xdrData: inout Data) throws {
      let discriminant = try Int32(xdrData: &xdrData)

      switch discriminant {
      case ManageAccountSpecificRuleAction.create.rawValue:
        let data = try CreateAccountSpecificRuleData(xdrData: &xdrData)
        self = .create(data)
      case ManageAccountSpecificRuleAction.remove.rawValue:
        let data = try RemoveAccountSpecificRuleData(xdrData: &xdrData)
        self = .remove(data)
      default:
        throw XDRErrors.unknownEnumCase
      }
    }

  }
  public enum ManageAccountSpecificRuleOpExt: XDRDiscriminatedUnion {
    case emptyVersion()

    public var discriminant: Int32 {
      switch self {
      case .emptyVersion: return LedgerVersion.emptyVersion.rawValue
      }
    }

    public func toXDR() -> Data {
      var xdr = Data()

      xdr.append(self.discriminant.toXDR())

      switch self {
      case .emptyVersion(): xdr.append(Data())
      }

      return xdr
    }

    public init(xdrData: inout Data) throws {
      let discriminant = try Int32(xdrData: &xdrData)

      switch discriminant {
      case LedgerVersion.emptyVersion.rawValue: self = .emptyVersion()
      default:
        throw XDRErrors.unknownEnumCase
      }
    }

  }
}
