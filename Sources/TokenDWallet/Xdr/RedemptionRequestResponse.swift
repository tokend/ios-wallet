// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

import Foundation

// === xdr source ============================================================

//  //: Result of successful application of `CreateRedemptionRequest` operation
//  struct RedemptionRequestResponse {
//      //: ID of a newly created reviewable request
//      uint64 requestID;
//      //: Indicates  whether or not the Redemption request was auto approved and fulfilled
//      bool fulfilled;
//  
//      //: ID of destination balance (may be freshly created)
//      BalanceID destinationBalanceID;
//      //: Code of an asset used in payment
//      AssetCode asset;
//      //: Amount sent by the sender
//      uint64 sourceSentUniversal;
//      //: Reserved for future use
//       union switch (LedgerVersion v)
//      {
//      case EMPTY_VERSION:
//          void;
//      }
//      ext;
//  };

//  ===========================================================================
public struct RedemptionRequestResponse: XDRCodable {
  public var requestID: Uint64
  public var fulfilled: Bool
  public var destinationBalanceID: BalanceID
  public var asset: AssetCode
  public var sourceSentUniversal: Uint64
  public var ext: RedemptionRequestResponseExt

  public init(
      requestID: Uint64,
      fulfilled: Bool,
      destinationBalanceID: BalanceID,
      asset: AssetCode,
      sourceSentUniversal: Uint64,
      ext: RedemptionRequestResponseExt) {

    self.requestID = requestID
    self.fulfilled = fulfilled
    self.destinationBalanceID = destinationBalanceID
    self.asset = asset
    self.sourceSentUniversal = sourceSentUniversal
    self.ext = ext
  }

  public func toXDR() -> Data {
    var xdr = Data()

    xdr.append(self.requestID.toXDR())
    xdr.append(self.fulfilled.toXDR())
    xdr.append(self.destinationBalanceID.toXDR())
    xdr.append(self.asset.toXDR())
    xdr.append(self.sourceSentUniversal.toXDR())
    xdr.append(self.ext.toXDR())

    return xdr
  }

  public init(xdrData: inout Data) throws {
    self.requestID = try Uint64(xdrData: &xdrData)
    self.fulfilled = try Bool(xdrData: &xdrData)
    self.destinationBalanceID = try BalanceID(xdrData: &xdrData)
    self.asset = try AssetCode(xdrData: &xdrData)
    self.sourceSentUniversal = try Uint64(xdrData: &xdrData)
    self.ext = try RedemptionRequestResponseExt(xdrData: &xdrData)
  }

  public enum RedemptionRequestResponseExt: XDRDiscriminatedUnion {
    case emptyVersion

    public var discriminant: Int32 {
      switch self {
      case .emptyVersion: return LedgerVersion.emptyVersion.rawValue
      }
    }

    public func toXDR() -> Data {
      var xdr = Data()

      xdr.append(self.discriminant.toXDR())

      switch self {
      case .emptyVersion: xdr.append(Data())
      }

      return xdr
    }

    public init(xdrData: inout Data) throws {
      let discriminant = try Int32(xdrData: &xdrData)

      switch discriminant {
      case LedgerVersion.emptyVersion.rawValue: self = .emptyVersion
      default:
        throw XDRErrors.unknownEnumCase
      }
    }

  }
}
