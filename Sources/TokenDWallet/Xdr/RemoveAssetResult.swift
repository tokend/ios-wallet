// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

import Foundation

// === xdr source ============================================================

//  //: Result of RemoveAsset operation application along with the result code
//  union RemoveAssetResult switch (RemoveAssetResultCode code) {
//      case SUCCESS:
//          RemoveAssetSuccess success;
//      default:
//          void;
//  };

//  ===========================================================================
public enum RemoveAssetResult: XDRDiscriminatedUnion {
  case success(RemoveAssetSuccess)

  public var discriminant: Int32 {
    switch self {
    case .success: return RemoveAssetResultCode.success.rawValue
    }
  }

  public func toXDR() -> Data {
    var xdr = Data()

    xdr.append(self.discriminant.toXDR())

    switch self {
    case .success(let data): xdr.append(data.toXDR())
    }

    return xdr
  }

  public init(xdrData: inout Data) throws {
    let discriminant = try Int32(xdrData: &xdrData)

    switch discriminant {
    case RemoveAssetResultCode.success.rawValue:
      let data = try RemoveAssetSuccess(xdrData: &xdrData)
      self = .success(data)
    default:
      throw XDRErrors.unknownEnumCase
    }
  }
}
