// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

import Foundation

// === xdr source ============================================================

//  //: CloseSwapResult is a result of CloseSwap operation application
//  union CloseSwapResult switch (CloseSwapResultCode code)
//  {
//  case SUCCESS:
//      CloseSwapSuccess success;
//  default:
//      void;
//  };

//  ===========================================================================
public enum CloseSwapResult: XDRDiscriminatedUnion {
  case success(CloseSwapSuccess)

  public var discriminant: Int32 {
    switch self {
    case .success: return CloseSwapResultCode.success.rawValue
    }
  }

  public func toXDR() -> Data {
    var xdr = Data()

    xdr.append(self.discriminant.toXDR())

    switch self {
    case .success(let data): xdr.append(data.toXDR())
    }

    return xdr
  }

  public init(xdrData: inout Data) throws {
    let discriminant = try Int32(xdrData: &xdrData)

    switch discriminant {
    case CloseSwapResultCode.success.rawValue:
      let data = try CloseSwapSuccess(xdrData: &xdrData)
      self = .success(data)
    default:
      throw XDRErrors.unknownEnumCase
    }
  }
}
