// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

import Foundation

// === xdr source ============================================================

//  //: Result of operation application
//  union UpdateDataOwnerResult switch (UpdateDataOwnerResultCode code)
//  {
//  case SUCCESS:
//      UpdateDataOwnerSuccess success;
//  default:
//      void;
//  };

//  ===========================================================================
public enum UpdateDataOwnerResult: XDRDiscriminatedUnion {
  case success(UpdateDataOwnerSuccess)

  public var discriminant: Int32 {
    switch self {
    case .success: return UpdateDataOwnerResultCode.success.rawValue
    }
  }

  public func toXDR() -> Data {
    var xdr = Data()

    xdr.append(self.discriminant.toXDR())

    switch self {
    case .success(let data): xdr.append(data.toXDR())
    }

    return xdr
  }

  public init(xdrData: inout Data) throws {
    let discriminant = try Int32(xdrData: &xdrData)

    switch discriminant {
    case UpdateDataOwnerResultCode.success.rawValue:
      let data = try UpdateDataOwnerSuccess(xdrData: &xdrData)
      self = .success(data)
    default:
      throw XDRErrors.unknownEnumCase
    }
  }
}
